package com.policymesh.servicegraph; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface DataFlowEdgeRepository extends JpaRepository<DataFlowEdge,Long>{List<DataFlowEdge> findBySourceServiceIdOrDestinationServiceId(Long source,Long destination);Optional<DataFlowEdge> findBySourceServiceIdAndDestinationServiceId(Long source,Long destination);}
