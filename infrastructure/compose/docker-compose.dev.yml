services:
  ai-service:
    environment:
      APP_ENV: development
      AI_MAX_RETRIES: 0
    volumes:
      - ../../ai-service:/app
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

