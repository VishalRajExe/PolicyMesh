# Reserved for future Istio resources

Future resources may include namespace injection labels, authorization policies, Envoy extension configuration, traffic policies, and observability settings. None are supplied now because they require a validated PolicyMesh enforcement API and operational decisions.
