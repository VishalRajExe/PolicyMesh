# Production PostgreSQL

Use a managed PostgreSQL offering or an operator-managed StatefulSet. Require encrypted backups, private networking, TLS, HA/failover testing, least-privilege database roles, rotation, monitoring, and a migration plan. Do not convert the single local Compose database into a production manifest.
