# Production Redis

Use a managed Redis service or a supported Redis operator/cluster. Configure authentication, TLS, persistence policy, eviction behavior, metrics, backup/recovery if needed, and a backend cache-fallback test. Redis remains non-authoritative.
