# Production Kafka

Use a managed Kafka platform or a supported operator such as Strimzi. Plan replication, partitions, retention, ACLs, TLS/SASL, schema/event compatibility, consumer lag monitoring, durable storage, and disaster recovery. The single local KRaft broker is not a production topology.
